/*
Task 2: The Garbage Trap
Write a quick main function that declares an integer count but does not initialize it. 
Try to print its value. (Notice what your compiler actually outputs when you run it).
*/

#include <stdio.h>

int main(){
    int counter;

    printf("The value of counter is %d\n", counter);

    return 0;
}

